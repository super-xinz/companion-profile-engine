# Copy these values into the B-side server's secret/config system.
# Never expose them in browser or mobile application code.
PROFILE_ENGINE_BASE_URL=https://replace-with-delivered-domain
PROFILE_ENGINE_TENANT_ID=replace-with-delivered-tenant-id
PROFILE_ENGINE_API_KEY=replace-via-secure-channel
PROFILE_ENGINE_TIMEOUT_MS=10000
